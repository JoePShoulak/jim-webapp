import React from 'react';

import {
    Card,
    CardContent,
    Typography } from "@material-ui/core";

const ShopContentApplet = () => {

    return(
        <Card>
            <CardContent>
                <Typography>Also nothing here yet</Typography>
            </CardContent>
        </Card>
    );
};

export default ShopContentApplet;
